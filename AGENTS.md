1. 如果我让你编写文档,那么编写完文档(文档统一使用中文),请询问我是否符合预期.并且可以向我提三个问题.
2. 如果你是基于文档进行开发,那么当你开发完成后,请更新文档.
3. 编写完代码后,一定要运行 npm run build 确保项目能够正常构建.
4. 注意代码的抽象封装分层,不要在一个文件中写太多逻辑.
5. 美术素材代码需要放置在单独的文件中(如单种枪械一个文件),切统一放置到assets目录下.
6. 请参考 `docs/TECH_OVERVIEW.md` 了解项目结构与架构。如果新增了模块或修改了旧模块的职责，必须同步更新该文档。
7. 涉及人物素材制作时，必须严格遵循 `docs/CHARACTER_GUIDE.md` 中的规范（32x32, 默认朝左, 至少8帧全身联动）。
8. 新增游戏按键控制时，必须在 `src/core/Input.js` 的 `keys` 对象和 `_initListeners` 中注册，否则按键不会被监听。
9. 制作武器需阅读 `docs/WEAPON_GUIDE.md` 中的武器配置规范。
10. 编写具体的方案文档, 文档放置在 `docs/feature/` 中.
11. 制作复杂的像素物体（非角色类）时，请参考 `docs/PIXEL_ART_GUIDE.md`，使用程序化绘制工具 `PixelDraw`，并根据需要扩展该工具库。